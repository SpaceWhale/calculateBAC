package converters;

import entities.imperial.ImperialDrinkingSession;
import entities.metric.MetricDrinkingSession;

public class ImperialMetricConverter {

    /**
     * all of the formulas online are in metric, so this is where we do the conversion--To prevent awkward duplicated
     * logic.
     * @param imperialDrinkingSession
     * @return
     */
    public MetricDrinkingSession metricDrinkingSessionFromImperialDrinkingSession(ImperialDrinkingSession imperialDrinkingSession) {
        return null;
    }
}
